# Deploy notes

- Streamlit Cloud: link your GitHub repo, set requirements.txt, and choose the streamlit command `streamlit run app/streamlit_app.py`.
- Render: create a Web Service, select Dockerfile or use the Procfile command above.
- Hugging Face Spaces: supports Gradio/Streamlit; ensure model downloads allowed.
