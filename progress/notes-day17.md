
### Create `notes-day17.md`

```md
# Day 17 — Caching, UX & Deploy Prep

- Added `model_singleton.py` to lazily load and cache ML model.
- Streamlit UX: generate button avoids automatic heavy calls; spinner + status messages.
- Added `Procfile` and `run_streamlit.ps1` for deployment & local run helpers.
- README updated with run and deploy instructions.
